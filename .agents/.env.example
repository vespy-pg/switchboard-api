# Cline Environment Configuration Example
# Copy this file to .env and customize for your local environment
# .env is git-ignored and will not be committed

# Database Configuration
DB_HOST=example_host
DB_PORT=5432
DB_NAME=example_name
DB_USER=agent_ro
DB_PASSWORD=example_password

# Gift Database Setup
# Password for the gift_user that will be created by setup_gift_database.sql
GIFT_USER_PASSWORD=example_gift_password

# AMS Database Names (comma-separated)
# These databases use SERIAL/INTEGER for IDs instead of NUMERIC(30,0)
AMS_DATABASES=pl_ams_cc,pl_ams_cc_uat

# Project User IDs
DEFAULT_USER_ID=
TEST_PATIENT_ID=

# Additional project-specific configuration can be added here
